# RepSuite Logo Assets

Place manual logo files here when they are ready:

- `repsuite-logo.png` for the full RepSuite wordmark/logo
- `repsuite-mark.png` for the icon-only mark
- `repsuite-logo-alt.png` for the alternate dark full-logo render

The app already references these paths and falls back to text if either file is not present yet.
